#!/usr/bin/env groovy

node('master') {
    stage('build') {
        try {
            sh "cd /var/lib/jenkins/workspace/"
        } catch {
            sh "cd /var/lib/jenkins/workspace/"
        } finaly {
            sh "cd /var/lib/jenkins/workspace/"
        }
    }
}
