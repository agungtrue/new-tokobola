pipeline {
    agent none

    stages {
        stage('Build') {
            steps {
                pwd
                // echo "Downloading dependencies"
                // checkout scm
                //
                // sh "composer install"
            }
        }
        stage('Test') {
            steps {
                // sh "./vendor/bin/phpunit"
            }
        }
    }
}
